<!DOCTYPE html>
<html lang="en-US">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<style>
		/* Brand palette */
		:root {
			--brand-blue: #0B5E8E;   /* primary deep blue */
			--brand-lime: #B8E215;   /* accent lime */
			/* Map legacy tokens used across templates */
			--brand-red: var(--brand-blue);
			--brand-yellow: var(--brand-lime);
		}
		img:is([sizes="auto" i], [sizes^="auto," i]) { contain-intrinsic-size: 3000px 1500px }
	</style>
	<title>Washology | Professional Pressure Washing & Soft Washing</title>
	<meta name="description" content="Washology restores curb appeal with expert pressure washing and soft wash services for homes and businesses. Driveways, roofs, siding, patios and more. Free estimates." />
	<link rel="canonical" href="/" />
	<meta property="og:locale" content="en_US" />
	<meta property="og:type" content="website" />
	<meta property="og:title" content="Washology | Professional Pressure Washing & Soft Washing" />
	<meta property="og:description" content="Expert exterior cleaning: pressure washing and soft washing for driveways, roofs, siding, patios, and storefronts." />
	<meta property="og:site_name" content="Washology" />
	<meta property="og:image:width" content="800" />
	<meta property="og:image:height" content="527" />
	<meta property="og:image:alt" content="a completed garage renovation project including new storage units, hex lighting, and epoxy flooring completed by Washology First Coast" />
	<meta property="og:image:type" content="image/png" />
	<meta name="twitter:card" content="summary_large_image" />
	<meta name="twitter:title" content="Washology | Professional Pressure Washing & Soft Washing" />
	<meta name="twitter:description" content="Exterior cleaning pros. Soft wash for roofs and siding, pressure wash for concrete and brick. Free estimates." />
	<link rel="alternate" type="application/rss+xml" title="Washology » Feed" href="/feed/" />
	<link rel="alternate" type="application/rss+xml" title="Washology » Comments Feed" href="/comments/feed/" />
	<style id='wp-emoji-styles-inline-css'>
		img.wp-smiley, img.emoji { display: inline !important; border: none !important; box-shadow: none !important; height: 1em !important; width: 1em !important; margin: 0 0.07em !important; vertical-align: -0.1em !important; background: none !important; padding: 0 !important; }
	</style>
	<link rel='stylesheet' id='wp-block-library-css' href='/wp-includes/css/dist/block-library/style.min.css_ver=6.8.2.css' media='all' />
	<style id='classic-theme-styles-inline-css'>.wp-block-button__link{color:#fff;background-color:#32373c;border-radius:9999px;box-shadow:none;text-decoration:none;padding:calc(.667em + 2px) calc(1.333em + 2px);font-size:1.125em}.wp-block-file__button{background:#32373c;color:#fff;text-decoration:none}</style>
	<style id='safe-svg-svg-icon-style-inline-css'>.safe-svg-cover{text-align:center}.safe-svg-cover .safe-svg-inside{display:inline-block;max-width:100%}.safe-svg-cover svg{height:100%;max-height:100%;max-width:100%;width:100%}</style>
	<link rel='stylesheet' id='main-css' href='/wp-content/themes/garageup/build/main.css_ver=b6dc510ac89b6f088fb6.css' media='all' />
	<link rel='stylesheet' id='nav-overrides-css' href='/wp-content/themes/garageup/build/nav-overrides.css' media='all' />
	<link rel='stylesheet' id='brand-overrides-css' href='/wp-content/themes/garageup/build/brand-overrides.css' media='all' />
	<!-- Late brand override to ensure palette wins over bundled CSS -->
	<style>
		:root { 
			--brand-blue: #0B5E8E !important; 
			--brand-lime: #B8E215 !important; 
			--brand-red: var(--brand-blue) !important; 
			--brand-yellow: var(--brand-lime) !important; 
		}
		/* Header banner and navbar */
		.banner-wrapper { background-color: var(--brand-blue) !important; }
		#header.navbar { border-bottom: 3px solid var(--brand-lime) !important; }
		#header .nav-link { color: var(--brand-blue) !important; }
		#header .nav-link:hover { color: #063e61 !important; }
		#header .dropdown-item:hover { background: var(--brand-lime) !important; color: #052B44 !important; }

		/* Global component overrides */
		.btn-primary, .btn-brand-primary { background-color: var(--brand-blue) !important; border-color: var(--brand-blue) !important; color: #fff !important; }
		.btn-outline-secondary { border-color: var(--brand-blue) !important; color: var(--brand-blue) !important; }
		.btn-outline-secondary:hover { background: var(--brand-lime) !important; border-color: var(--brand-lime) !important; color: #052B44 !important; }
		.badge, .label { background: var(--brand-lime) !important; color: #052B44 !important; }
		.bg-warning, .bg-danger, .bg-primary { background-color: var(--brand-blue) !important; }
		.text-danger, .text-primary { color: var(--brand-blue) !important; }
		.border-primary { border-color: var(--brand-lime) !important; }
	</style>
	<script src="/wp-includes/js/jquery/jquery.min.js_ver=3.7.1" id="jquery-core-js"></script>
	<script src="/wp-includes/js/jquery/jquery-migrate.min.js_ver=3.4.1" id="jquery-migrate-js"></script>
	<link rel="https://api.w.org/" href="/wp-json/" />
	<link rel="alternate" title="JSON" type="application/json" href="/wp-json/wp/v2/pages/8" />
	<link rel='shortlink' href='/' />
	
	<link rel="icon" href="/wp-content/uploads/2025/04/favicon-32x32.png" sizes="32x32" />
	<link rel="icon" href="/wp-content/uploads/2025/04/favicon-192x192.png" sizes="192x192" />
	<link rel="apple-touch-icon" href="/wp-content/uploads/2025/04/favicon-192x192.png" />
	<meta name="msapplication-TileImage" content="https://garageup.com/wp-content/uploads/2025/04/favicon-192x192" />
	<style>
		/* Global top banner styles */
		.banner-wrapper { background-color: var(--brand-red); }
		.banner-wrapper .banner-header { 
			margin: 0; 
			text-align: center; 
			text-transform: uppercase; 
			font-weight: 700; 
			letter-spacing: .02em;
			padding: 8px 0;
		}
		.banner-wrapper .banner-header a { 
			color: #fff; 
			text-decoration: none; 
			display: inline-block; 
			padding: 4px 10px;
		}
		.banner-wrapper .banner-header a:hover { text-decoration: underline; color: #fff; }
		/* Header layout adjustments */
		#header .container { position: relative; }
		#header .mbl-nav-wrapper { display: flex; align-items: center; justify-content: center; position: relative; width: 100%; }
		#header .mbl-nav-wrapper .navbar-toggler { position: absolute; right: 0; }
		/* Desktop nav: split menus with centered logo */
		@media (min-width: 768px) {
		  #navbar .navbar-nav { width: 100%; display: flex; align-items: center; justify-content: center; gap: 16px; }
		  #navbar .navbar-nav .left-menu, 
		  #navbar .navbar-nav .right-menu { display: flex; align-items: center; gap: 12px; }
		  #navbar .navbar-nav .center-brand { display: flex !important; align-items: center; justify-content: center; padding: 0 8px; }
		}

		/* Brand theming for navbar */
		#header.navbar { border-bottom: 3px solid var(--brand-yellow); }
		#header .nav-link { color: var(--brand-red); font-weight: 600; }
		#header .nav-link:hover, #header .nav-link:focus { color: #063e61; }
		#header .dropdown-menu { border-radius: 10px; border-color: rgba(11,94,142,.15); }
		#header .dropdown-item:hover, #header .dropdown-item:focus { background: var(--brand-yellow); color: #052B44; }

		/* Primary buttons in header context */
		.btn.btn-brand-primary { background-color: var(--brand-red) !important; border-color: var(--brand-red) !important; color: #fff; }
		.btn.btn-brand-primary:hover { filter: brightness(1.05); box-shadow: 0 6px 16px rgba(11,94,142,.28); }

		/* Site loader overlay */
		#site-loader { position: fixed; inset: 0; background: var(--brand-yellow); display: flex; align-items: center; justify-content: center; z-index: 9999; transition: opacity .3s ease, visibility .3s ease; }
		#site-loader img { width: 170px; height: auto; filter: drop-shadow(0 2px 6px rgba(0,0,0,.15)); }
		#site-loader.hidden { opacity: 0; visibility: hidden; }
	</style>    
</head>
<body class="<%= typeof bodyClass !== 'undefined' && bodyClass ? bodyClass : 'home wp-singular page-template page-template-templates page-template-landing page-template-templateslanding-php page page-id-8 wp-theme-resi-franchise wp-child-theme-garageup' %>">
	<!-- Site Loader -->
	<div id="site-loader" aria-hidden="true" role="presentation">
		<img src="/pictures/logo/Washology.png" alt="Washology" />
	</div>
	<script>
		window.addEventListener('load', function () {
			var el = document.getElementById('site-loader');
			if (!el) return;
			el.classList.add('hidden');
			setTimeout(function(){ try { el.remove(); } catch(e) { if (el && el.parentNode) el.parentNode.removeChild(el); } }, 400);
		});
	</script>
	<!-- Force header Contact links to navigate (do not open ZIP modal) -->
	<script>
	(function(){
	  function sanitizeLink(a){
	    try {
	      a.removeAttribute('data-bs-toggle');
	      a.removeAttribute('data-bs-target');
	      // Stop any other scripts from intercepting the click; allow default navigation to proceed
	      a.addEventListener('click', function(e){ e.stopImmediatePropagation(); }, true);
	    } catch(_) {}
	  }
	  document.addEventListener('DOMContentLoaded', function(){
	    // Header nav and banner contact links
	    document.querySelectorAll('header a[href="/contact-us"]').forEach(sanitizeLink);
	    var banner = document.querySelector('.banner-header a');
	    if (banner && /\/contact-us$/.test(banner.getAttribute('href')||'')) sanitizeLink(banner);
	  });
	})();
	</script>
	<a href="#main" class="visually-hidden-focusable">Skip to main content</a>

	<!-- Global ZIP capture modal (available on all pages) -->
	<div id="zip-modal-backdrop" class="zip-modal-backdrop" aria-hidden="true" style="display:none; position:fixed; inset:0; background: rgba(0,0,0,.45); z-index: 2000; align-items:center; justify-content:center;">
	  <div class="zip-modal" role="dialog" aria-modal="true" aria-labelledby="zip-modal-title" style="background:#fff; width:min(92vw,520px); border-radius:16px; box-shadow:0 24px 48px rgba(0,0,0,.25); overflow:hidden;">
	    <header id="zip-modal-title" style="padding:1rem 1.25rem; font-weight:800; font-size:1.1rem; background: var(--brand-yellow); color: var(--brand-blue);">Enter your ZIP code</header>
	    <div class="body" style="padding:1rem 1.25rem;">
	      <div class="zip-input-group" style="display:flex; align-items:center; gap:.5rem; border:1px solid #ddd; border-radius:10px; padding:.5rem .75rem;">
	        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true"><path d="M10 2a8 8 0 1 1 0 16 8 8 0 0 1 0-16Zm11 20-6-6" stroke="#888" stroke-width="2" stroke-linecap="round"/></svg>
	        <input type="text" id="zip-modal-input" inputmode="numeric" maxlength="5" placeholder="12345" autocomplete="postal-code" aria-label="ZIP code" style="flex:1; border:0; outline:0; font-size:1rem;" />
	      </div>
	      <div id="zip-suggest-modal" class="zip-suggest-modal" role="listbox" aria-label="ZIP suggestions" style="display:none; margin-top:.5rem; border:1px solid #e5e5e5; border-radius:10px; overflow:hidden; background:#fff;"></div>
	      <div id="zip-hint" style="margin-top:.5rem; font-size:.9rem; color:#666;"></div>
	    </div>
	    <div class="actions" style="display:flex; gap:.5rem; justify-content:flex-end; padding: .75rem 1.25rem 1.25rem;">
	      <button type="button" class="btn-cancel" id="zip-cancel" style="background:#f3f3f3; border:1px solid #ddd; border-radius:8px; padding:.5rem .9rem;">Cancel</button>
	      <button type="button" class="btn-continue" id="zip-continue" style="background: var(--brand-yellow); color: var(--brand-blue); border:1px solid var(--brand-yellow); border-radius:8px; padding:.5rem .9rem; font-weight:700;">Continue</button>
	    </div>
	  </div>
	</div>
	<script>
	(function(){
	  if (window.__zipModalGlobalInitialized) return; 
	  window.__zipModalGlobalInitialized = true;
	  try {
	    var backdrop = document.getElementById('zip-modal-backdrop');
	    var input = document.getElementById('zip-modal-input');
	    var suggest = document.getElementById('zip-suggest-modal');
	    var btnCancel = document.getElementById('zip-cancel');
	    var btnGo = document.getElementById('zip-continue');
	    var hint = document.getElementById('zip-hint');
	    var selected = null;

	    function hideSuggest(){ if (suggest){ suggest.style.display='none'; suggest.innerHTML=''; } }
	    function showLoading(){ if (!suggest) return; suggest.innerHTML = '<div class="item" style="padding:.5rem .75rem; color:#444;">Searching…</div>'; suggest.style.display='block'; }
	    function showSuggest(items){
	      if (!suggest) return;
	      suggest.innerHTML = '';
	      if (!items || !items.length) { hideSuggest(); return; }
	      items.forEach(function(item){
	        var div = document.createElement('div');
	        div.className = 'item';
	        div.setAttribute('role','option');
	        div.style.cssText = 'display:flex;align-items:center;gap:.5rem;padding:.5rem .75rem;cursor:pointer;';
	        div.innerHTML = '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true"><path d="M12 2C8.14 2 5 5.14 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.86-3.14-7-7-7Zm0 9.5a2.5 2.5 0 1 1 0-5 2.5 2.5 0 0 1 0 5Z" fill="currentColor"/></svg> ' + (item.label || item);
	        div.addEventListener('click', function(){ selected = item; hint.textContent = item.label || ''; hideSuggest(); });
	        suggest.appendChild(div);
	      });
	      suggest.style.display = 'block';
	    }
	    function formatLabel(zip, city, state){
	      if (city && state) return city + ', ' + state + ' ' + zip + ', USA';
	      return zip + ', USA';
	    }
	    function lookup(v){
	      var z = String(v||'').replace(/\D/g,'').slice(0,5);
	      if (z.length !== 5) { hideSuggest(); selected=null; return; }
	      showLoading();
	      fetch('https://api.zippopotam.us/us/' + z)
	        .then(function(r){ if(!r.ok) throw new Error('nf'); return r.json(); })
	        .then(function(data){
	          var place = (data.places && data.places[0]) || null;
	          var city = place ? place['place name'] : '';
	          var state = place ? place['state abbreviation'] : '';
	          selected = { zip: z, city: city, state: state, label: formatLabel(z, city, state) };
	          showSuggest([selected]);
	        })
	        .catch(function(){ hideSuggest(); selected=null; });
	    }
	    function show(){ if (!backdrop) return; backdrop.style.display='flex'; selected=null; input && (input.value=''); hint && (hint.textContent=''); hideSuggest(); setTimeout(function(){ try { input && input.focus(); } catch(_) {} }, 30); }
	    function hide(){ if (!backdrop) return; backdrop.style.display='none'; }
	    window.showZipModal = show;

	    if (input){
	      var t; 
	      input.addEventListener('input', function(){ var v = input.value.replace(/\D/g,'').slice(0,5); input.value=v; clearTimeout(t); if (v.length===5){ t=setTimeout(function(){ lookup(v); }, 120); } else { hideSuggest(); } });
	      input.addEventListener('blur', function(){ setTimeout(hideSuggest, 150); });
	    }
	    btnCancel && btnCancel.addEventListener('click', hide);
	    btnGo && btnGo.addEventListener('click', function(){
	      var v = (selected && selected.zip) || (input && input.value || '').replace(/\D/g,'').slice(0,5);
	      if (!v || v.length !== 5){ if (hint) hint.textContent = 'Please enter a valid 5-digit ZIP'; return; }
	      var url = '/contact-us?zip=' + v;
	      if (selected){ if (selected.city) url += '&city=' + encodeURIComponent(selected.city); if (selected.state) url += '&state=' + encodeURIComponent(selected.state); }
	      window.location.href = url;
	    });

	    // Global delegation: ONLY open ZIP modal for explicit [data-zip-trigger]
	    document.addEventListener('click', function(ev){
	      var el = ev.target && ev.target.closest ? ev.target.closest('[data-zip-trigger]') : null;
	      if (!el) return;
	      ev.preventDefault();
	      ev.stopImmediatePropagation();
	      show();
	    }, true);

	    // Close on backdrop click or ESC
	    backdrop && backdrop.addEventListener('click', function(e){ if (e.target === backdrop) hide(); });
	    document.addEventListener('keydown', function(e){ if (backdrop && backdrop.style.display==='flex' && e.key==='Escape') hide(); });
	  } catch(e) { /* no-op */ }
	})();
	</script>

	<div id="wrapper">
		<header>
			<div class="banner-wrapper">
				<div class="col-sm-12 banner-wrapper">
					<h6 class="banner-header"><a href="#" data-zip-trigger="1" role="button">Get Free Estimate</a></h6>
				</div>
			</div>
			<nav id="header" class="navbar navbar-expand-md justify-content-center navbar-light bg-light">
				<div class="container">
					<div class="mbl-nav-wrapper">
						<a class="navbar-brand d-md-none" href="/" title="Washology" rel="home">
							<img src="/pictures/logo/Washology.png" alt="Washology" />
						</a>
						<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar" aria-controls="navbar" aria-expanded="false" aria-label="Toggle navigation">
							<span class="navbar-toggler-icon"></span>
						</button>
					</div>
					<div id="navbar" class="collapse navbar-collapse">
						<ul id="menu-main-menu-new" class="navbar-nav">
							<ul id="menu-main-menu" class="navbar-nav left-menu">
								<li class="menu-item menu-item-type-post_type menu-item-object-page nav-item"><a title="About Us" href="/about-us" class="nav-link">About Us</a></li>
								<li class="menu-item menu-item-type-post_type menu-item-object-page nav-item"><a title="Contact Us" href="/contact-us" class="nav-link">Contact Us</a></li>
							</ul>
							<li class="center-brand d-none d-md-flex">
								<a class="navbar-brand" href="/" title="Washology" rel="home">
									<img src="/pictures/logo/Washology.png" alt="Washology" />
								</a>
							</li>
							<ul id="menu-main-menu-1" class="navbar-nav right-menu">
								<li class="menu-item menu-item-type-custom menu-item-object-custom menu-item-has-children nav-item dropdown">
									<a title="Services" href="#" data-bs-toggle="dropdown" class="nav-link dropdown-toggle" aria-expanded="false">Services <span class="caret"></span></a>
									<ul role="menu" class="dropdown-menu">
										<li class="menu-item menu-item-type-post_type menu-item-object-resi_service"><a title="Weekly Service" href="#" class="dropdown-item" data-bs-toggle="modal" data-bs-target="#scheduleModal">Weekly Service</a></li>
										<li class="menu-item menu-item-type-post_type menu-item-object-resi_service"><a title="Chemical Balancing" href="#" class="dropdown-item" data-bs-toggle="modal" data-bs-target="#scheduleModal">Chemical Balancing</a></li>
										<li class="menu-item menu-item-type-post_type menu-item-object-resi_service"><a title="Filter Cleaning" href="#" class="dropdown-item" data-bs-toggle="modal" data-bs-target="#scheduleModal">Filter Cleaning</a></li>
										<li class="menu-item menu-item-type-post_type menu-item-object-resi_service"><a title="Green Pool Cleanup" href="#" class="dropdown-item" data-bs-toggle="modal" data-bs-target="#scheduleModal">Green Pool Cleanup</a></li>
										<li class="menu-item menu-item-type-post_type menu-item-object-resi_service"><a title="Equipment Repairs" href="#" class="dropdown-item" data-bs-toggle="modal" data-bs-target="#scheduleModal">Equipment Repairs</a></li>
										<li class="menu-item menu-item-type-post_type menu-item-object-resi_service"><a title="Pump &amp; Heater Service" href="#" class="dropdown-item" data-bs-toggle="modal" data-bs-target="#scheduleModal">Pump &amp; Heater Service</a></li>
									</ul>
								</li>
								<li class="menu-item menu-item-type-custom menu-item-object-custom nav-item"><a title="Blog" href="/blog" class="nav-link">Blog</a></li>
							</ul>
						</ul>
					</div><!-- /.navbar-collapse -->
				</div><!-- /.container -->
			</nav><!-- /#header -->
		</header>
		<main id="main" class="">
